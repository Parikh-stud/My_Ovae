# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.

## Environment Variables

Set the following environment variable in your `.env.local` file for local development and in your Vercel project settings for deployments:

- `GEMINI_API_KEY`: Google Gemini API key used by the AI nutrition analysis flow.
